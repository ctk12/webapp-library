export enum roleType {
  USER = "USER",
  ADMIN = "ADMIN",
}

export enum statusType {
  available = "available",
  unavailable = "unavailable",
}

export enum transactionStatusType {
  borrowed = "borrowed",
  returned = "returned",
}
